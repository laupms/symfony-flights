{% extends 'base.html.twig' %}

{% block title %}flights - Nouveau vol{% endblock %}

{% block body %}
<section class="container justify-content-center>
    <div class="row">
      
        <div class="col text-left">
             <h1>Création d'un nouveau vol</h1>
            {{ include('admin/flights/_form.html.twig') }}
        </div>
        <div class="return d-flex justify-content-end">
            <button class="btn btn-secondary m-3">
                <a href="{{ path('admin_index') }}" style="text-decoration:none; color:#fff;">
                    <i class="fa-solid fa-rotate-left" style="color: #fff;"></i>
                Retour à la liste</a>
            </button>
        </div>
      
    </div>
</section>
   
{% endblock %}
